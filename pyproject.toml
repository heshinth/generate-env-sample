[project]
name = "generate-env-sample"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Heshinth P", email = "heshinth@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "typer>=0.17.4",
]

[project.scripts]
generate-env-sample = "generate_env_sample:main"

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"
